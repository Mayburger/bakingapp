<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:mContext="nacoda.android.com.popularmovies.fragments.Top">

    <!-- TODO: Update blank fragment layout -->

    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshRecipes"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/rvRecipesList"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </android.support.v4.widget.SwipeRefreshLayout>

    <ImageView
        android:id="@+id/ivError"
        android:layout_width="@dimen/errorImageViewLargeWidth"
        android:layout_height="@dimen/errorImageViewLargeHeight"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:src="@drawable/error_image" />

    <TextView
        android:id="@+id/tvError"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_below="@id/ivError"
        android:layout_centerHorizontal="true"
        android:text="@string/error"
        android:textColor="@color/colorPrimary"
        android:textSize="20sp" />


</RelativeLayout>
